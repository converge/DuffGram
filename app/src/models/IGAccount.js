/*const mongoose = require('mongoose')

const DuffGramSchema = new mongoose.Schema({
    username: {
        required: true,
    },
    createdAt: {
        type: Date,
        default: Date.now,
    }
})

mongoose.model('IGAccount', DuffGramSchema)
*/
